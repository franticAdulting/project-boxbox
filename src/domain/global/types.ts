export interface JobContext {
  traceId: string
}

export enum GatherClass {
  Miner = 'miner',
  Botanist = 'botanist',
}

export enum Region {
  TheBlackShroud = 'the-black-shroud',
}

export enum Zone {
  NorthShroud = 'north-shroud',
}
