generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("POSTGRES_URL")
}

model user {
  id    String  @id @unique(map: "user_id_unique") @db.Uuid
  email String?
}
